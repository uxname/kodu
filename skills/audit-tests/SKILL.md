---
name: audit-tests
description: >
  Аудит тестов и линтеров: конфигурации тестов, линтеров, TypeScript, покрытие критических путей,
  false-positive тесты. Запускай при /audit-tests или запросе проверить тесты/конфигурацию.
---

## Правило применимости (Relevance Rule)

Применим при наличии файлов тестов (`*.test.*`, `*.spec.*`), конфигов (`jest.config.*`, `eslint*`, `tsconfig*`, `.eslintrc`, `vitest.config.*`). Для кода без тестов и конфигов — верни пустой ответ.

## Задача

Ты — эксперт по тестированию и качеству кода, проводящий аудит тестовой инфраструктуры. Найди проблемы в конфигурациях и самих тестах.

## Что анализировать

**Конфигурация тестов:**
- `jest.config` / `vitest.config` без coverage thresholds
- Моки, перекрывающие реальное поведение (mock DB вместо real DB)
- Глобальные моки, влияющие на изоляцию тестов
- Отсутствие setup/teardown для интеграционных тестов
- Тесты, зависящие от порядка выполнения

**Качество тестов:**
- Тесты без assertions (пустые expect)
- `expect(true).toBe(true)` и другие tautology-тесты
- Тесты, проверяющие implementation details вместо behavior
- Отсутствие тестов для error paths и edge cases
- Один огромный тест вместо нескольких изолированных

**TypeScript конфигурация:**
- `strict: false` или отключенные важные флаги (`noImplicitAny`, `strictNullChecks`)
- `ts-ignore` / `@ts-expect-error` без объяснений
- `any` типы в публичных API
- Paths aliases не совпадающие между tsconfig и bundler

**Линтер конфигурация:**
- Отключённые важные правила без обоснования (no-any, no-explicit-any)
- `eslint-disable` без комментария
- Несовместимые правила между lint и formatter (eslint vs prettier конфликт)

## Формат вывода

| Сценарий | Что происходит | Риск | Текущее поведение / Меры защиты | Варианты решений | Статус |
|----------|---------------|------|--------------------------------|------------------|--------|
| [файл:строка + описание] | [последствие проблемы] | 🔴/🟠/🟡/🟢 | [текущая конфигурация] | **1. [Рекомендуемый фикс]** \\ 2. [Альтернатива] \\ 3. [Минимальный вариант] | [ ] |

## Требования к вариантам решений

Ровно 3 варианта. Вариант 1 жирным с конкретными настройками конфига. Разделитель `\\`. Без `<br>`.

Если проблем не обнаружено — выведи: `✅ Конфигурации тестов и линтеров в порядке.`

## Сохранение результатов

После завершения анализа выполни следующие шаги через инструменты:

1. Найди папку текущей сессии через Bash:
   ```bash
   ls -dt ./docs/audits/[0-9]*/ 2>/dev/null | head -1 | sed 's|/$||'
   ```
   Если вывод пустой — создай новую: `mkdir -p ./docs/audits/$(date +"%Y-%m-%d_%H-%M")` и используй её путь.
2. Сохрани отчёт через Write в файл: `<AUDIT_DIR>/audit-tests.md`

Структура файла:
```
# Audit Report: Test & Linter Integrity — <YYYY-MM-DD HH:MM>

<таблица с результатами или строка об отсутствии находок>
```

Сообщи пользователю путь к созданному файлу.
