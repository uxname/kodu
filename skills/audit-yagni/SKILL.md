---
name: audit-yagni
description: >
  Аудит over-engineering и YAGNI: лишние абстракции, преждевременная оптимизация,
  неиспользуемый код. Запускай при /audit-yagni или запросе найти переусложнения.
---

## Правило применимости (Relevance Rule)

Применим к любому production-коду с классами, паттернами проектирования или абстракциями. Для простых скриптов-утилит без архитектуры — применяй только к явным over-engineering паттернам.

## Задача

Ты — эксперт по clean code, проводящий аудит избыточной сложности. Найди код, который усложняет систему без реальной пользы.

## Что анализировать

**Преждевременные абстракции:**
- Интерфейс/абстрактный класс с единственной реализацией (без тестов, где mock нужен)
- Factory/Builder/Strategy для объекта с 1-2 вариантами создания
- Generic-типы с одним конкретным использованием
- Event bus/pub-sub для прямых вызовов между 2 модулями

**Мёртвый код:**
- Экспортируемые функции/классы без единого импорта
- Закомментированный код
- Feature flags, всегда равные true/false
- TODO/FIXME старше 6 месяцев без прогресса

**Излишняя гибкость:**
- Конфигурируемость параметров, которые никогда не меняются
- Plugin-архитектура для системы без плагинов
- Versioning API для внутренних сервисов
- Опциональные параметры, всегда передаваемые с одним значением

**Cargo-cult паттерны:**
- Repository pattern поверх ORM без необходимости абстракции
- Service layer, просто проксирующий вызовы без логики
- DTO для объектов, идентичных entity
- Adapter для кода, который не нужно адаптировать

## Формат вывода

| Сценарий | Что происходит | Риск | Текущее поведение / Меры защиты | Варианты решений | Статус |
|----------|---------------|------|--------------------------------|------------------|--------|
| [файл:строка + паттерн] | [почему это лишнее] | 🟠 Высокий / 🟡 Средний / 🟢 Низкий | [текущее использование] | **1. [Упрощение]** \\ 2. [Частичное упрощение] \\ 3. [Оставить, задокументировать причину] | [ ] |

## Требования к вариантам решений

Ровно 3 варианта. Вариант 1 жирным — конкретное удаление/упрощение. Разделитель `\\`. Без `<br>`. Вариант 3 допускает сохранение при наличии обоснования.

Если проблем не обнаружено — выведи: `✅ Over-engineering не обнаружен.`

## Сохранение результатов

После завершения анализа выполни следующие шаги через инструменты:

1. Получи timestamp через Bash: `date +"%Y-%m-%d_%H-%M"`
2. Создай директорию через Bash: `mkdir -p ./docs/audits/`
3. Сохрани отчёт через Write в файл: `./docs/audits/<TIMESTAMP>_audit-yagni.md`

Структура файла:
```
# Audit Report: Over-engineering & YAGNI — <YYYY-MM-DD HH:MM>

<таблица с результатами или строка об отсутствии находок>
```

Сообщи пользователю путь к созданному файлу.
