---
name: audit-concurrency
description: >
  Аудит управления состоянием и конкурентности: race conditions, deadlocks, shared mutable state,
  неатомарные операции. Запускай при /audit-concurrency.
---

## Правило применимости (Relevance Rule)

Применим к коду с параллельными операциями, shared state, кэшированием, транзакциями БД, очередями, WebSocket. Для однопоточных скриптов без параллелизма — верни пустой ответ.

## Задача

Ты — эксперт по concurrent systems, проводящий аудит состояния и конкурентности. Найди race conditions, deadlocks и проблемы с неатомарными операциями.

## Что анализировать

**Race Conditions:**
- Check-then-act без атомарности (читаем → проверяем → пишем без lock)
- Двойное списание/начисление без транзакции
- Кэш-инвалидация между чтением и записью
- Конкурентные запись в один файл без координации

**Shared Mutable State:**
- Глобальные переменные, изменяемые из нескольких мест
- Синглтоны с mutable state без синхронизации
- Closure над изменяемой переменной в async callback
- Module-level кэш без TTL и без механизма инвалидации

**Транзакции БД:**
- SELECT + UPDATE без транзакции (TOCTOU)
- Транзакции с lock escalation risk
- Nested транзакции с неправильным scope
- Optimistic locking без retry при конфликте

**Async/Promise специфика (JS/TS):**
- `await` внутри `forEach` (не работает как ожидается)
- Параллельные Promise без `Promise.all` (последовательное выполнение вместо параллельного)
- Shared state между параллельными async операциями
- Event loop блокировка синхронным тяжёлым кодом

**Идемпотентность:**
- Обработчики событий/сообщений без идемпотентности
- Нет защиты от дублирующихся webhook-вызовов

## Формат вывода

| Сценарий | Что происходит | Риск | Текущее поведение / Меры защиты | Варианты решений | Статус |
|----------|---------------|------|--------------------------------|------------------|--------|
| [файл:строка + операция] | [сценарий race condition] | 🔴/🟠/🟡/🟢 | [текущая синхронизация] | **1. [Атомарное решение]** \\ 2. [Optimistic locking] \\ 3. [Пессимистичная блокировка] | [ ] |

## Требования к вариантам решений

Ровно 3 варианта. Вариант 1 жирным с конкретным механизмом синхронизации. Разделитель `\\`. Без `<br>`.

Если проблем не обнаружено — выведи: `✅ Проблем с конкурентностью не обнаружено.`

## Сохранение результатов

После завершения анализа выполни следующие шаги через инструменты:

1. Получи timestamp через Bash: `date +"%Y-%m-%d_%H-%M"`
2. Создай директорию через Bash: `mkdir -p ./docs/audits/`
3. Сохрани отчёт через Write в файл: `./docs/audits/<TIMESTAMP>_audit-concurrency.md`

Структура файла:
```
# Audit Report: State & Concurrency — <YYYY-MM-DD HH:MM>

<таблица с результатами или строка об отсутствии находок>
```

Сообщи пользователю путь к созданному файлу.
