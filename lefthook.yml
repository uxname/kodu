pre-commit:
  parallel: false
  commands:
    fmt:
      glob: "*.go"
      run: gofmt -w {staged_files}
      stage_fixed: true
    vet:
      run: CGO_ENABLED=1 go vet ./...

pre-push:
  parallel: false
  commands:
    test:
      run: CGO_ENABLED=1 go test ./...
