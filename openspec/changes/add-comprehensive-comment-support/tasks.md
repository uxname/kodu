## 1. Implementation
- [ ] 1.1 Расширить `CleanerService.collectCommentRanges`, чтобы он распознавал не только JS-комментарии, но и блоки `/* ... */`, любые встроенные HTML-комментарии и другие языковые конструкции.
- [ ] 1.2 Убедиться, что `CleanerService.shouldRemove` по-прежнему уважает `keepJSDoc` и набор встроенных/пользовательских подпараметров.
- [ ] 1.3 Добавить юнит-тесты для `clean`-команды, которые покрывают удаление многострочных и HTML-комментариев, включая проверку `--dry-run` и гибкого приставления контекста.
- [ ] 1.4 Обновить документацию команды (описание/help `clean`), чтобы упомянуть новые типы комментариев и их ограничения.

## 2. Validation
- [ ] 2.1 Запустить `npm run lint` и `npm run test`, чтобы убедиться, что новая логика не нарушила существующее поведение.
