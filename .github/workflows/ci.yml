name: CI

on:
  push:
    branches: [master, go-migration]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.26"
      - name: Build (CGO for tree-sitter)
        env:
          CGO_ENABLED: "1"
        run: go build ./...
      - name: Test
        env:
          CGO_ENABLED: "1"
        run: go test ./...
      - name: Lint
        uses: golangci/golangci-lint-action@v7
        with:
          version: v2.12.2
        env:
          CGO_ENABLED: "1"
